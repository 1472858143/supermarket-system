spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/market?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:qwer@1234}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.supermarket.inventory: DEBUG
